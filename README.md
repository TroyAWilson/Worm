## Worm - a recreation of the classic game Snake
**Worm requires Linux curses library**

To run worm:
```
make //which will compile the executable
./game.x [width] [height] // width and height are required
```
